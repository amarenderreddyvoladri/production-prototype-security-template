services:
  eureka-server:
    build: .

    container_name: eureka-server

    ports:
      - "8761:8761"

    healthcheck:
      test: ["CMD-SHELL", "wget --spider -q http://localhost:8761/actuator/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5